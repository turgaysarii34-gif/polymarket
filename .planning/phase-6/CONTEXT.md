# Phase 6 Context

Goal: support replaying many saved snapshots to produce longitudinal research evidence.

Need:
- iterate a snapshot directory
- replay each snapshot through existing pipeline
- return aggregate counts
- keep deterministic and local
